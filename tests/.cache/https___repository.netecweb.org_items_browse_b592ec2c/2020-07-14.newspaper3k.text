Search using this query type:

Keyword

Boolean

Exact match

Keyword will search multiple words together AND separately (search 3 characters minimum), but will exclude words found in more than half the records (including 'Ebola'). Choose 'Exact match' above to search common terms (or 2 characters).